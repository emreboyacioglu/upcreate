DATABASE_URL="postgresql://upcreate:upcreate_secret@localhost:5432/upcreate?schema=public"
PORT=4000
NODE_ENV=development
# Production: comma-separated origins, e.g. https://upcreate.co,https://www.upcreate.co
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Auth (MVP)
JWT_SECRET=change-me-in-production
# Optional: required body field adminRegisterSecret must match to register ADMIN users
# ADMIN_REGISTER_SECRET=
